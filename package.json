{
  "name": "xendit_trial_page",
  "version": "2.0.1",
  "description": "Simple text that listens on localhost:8080",
  "main": "server.js",
  "scripts": {
    "test": "echo \"no test specified\" && exit 0",
    "start": "node server.js"
  }
}
